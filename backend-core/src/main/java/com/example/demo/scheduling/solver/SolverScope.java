package com.example.demo.scheduling.solver;

/** Phạm vi dữ liệu đưa vào model (mở rộng §7.7). */
public enum SolverScope {
    /** Toàn bộ LHP thuộc {@code hoc_ky_id} trong build domain. */
    PER_HOC_KY
}
