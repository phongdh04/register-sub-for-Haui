# Sao chép thành .env.local nếu cần.
# Mặc định (dev): để trống hoặc không tạo file — frontend dùng proxy Vite /api → http://localhost:8080.
# Nếu không dùng proxy, đặt rõ URL backend:
# VITE_API_BASE_URL=http://localhost:8080
